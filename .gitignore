.DS_Store

.gocache/
.gomodcache/
__pycache__/

bin/
/aipostex
aipostex-linux
aipostex-linux-*
aipostex-darwin-*
aipostex-windows-*
*.test
*.code-workspace

# Coverage
coverage.out
coverage-*.out
*cover*.out

# Checkpoints
aipostex-checkpoint-*.jsonl

# Generated engagement output / dev scratch (never commit to the public repo)
campaign-output/
test-results/
ctf-captures/
internal-docs/
lab-report.html
lab-scan.json

# Handoff docs (local only)
HANDOFF-*.md
site/
tmp/

# Separate repo — kept in workspace for convenience
aipostex-lab/

# DEF CON con-kit lives in /con-kit/ — VISIBLE in the workspace, canonical source (~/aipostex-operator's
# local mkdocs symlinks to it). It may be committed to the PRIVATE dev repo (aipostex-dev) but MUST be
# EXCLUDED from the public release publish, like the lab's review-for-recording. Do NOT let it reach the
# public repo. The stale root duplicates below stay ignored (superseded by con-kit/).
/cards/
/rtv/
/defcon-34-*.md
/deck-*.md
/rehearsal-commands.md
